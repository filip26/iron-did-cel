package com.apicatalog.di;

public interface ProofReader {

    // no context -> proof : on|off
    // accepts no context | or/and listed contexts only + condition (anywhere, starts with
    // String getProofProperty(Map<String, Object> document);
    
    // iterative
}
